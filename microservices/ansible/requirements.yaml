# Ansible requirements for microservices deployment
---
collections:
  - name: kubernetes.core
    version: ">=2.4.0"
  - name: community.kubernetes
    version: ">=2.0.0"
  - name: amazon.aws
    version: ">=5.0.0"

python_requires: ">=3.9"

roles: []
